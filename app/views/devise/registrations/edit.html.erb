<div class="w-full min-h-[100vh] pt-24">
  <div class="max-w-xl w-full mx-auto shadow-lg bg-neutral-300 rounded-lg py-8 text-neutral-800">
    <h2 class="text-xl mb-4 text-center">Edytuj <%= resource_name.to_s.humanize %></h2>
    <div class="w-3/4 mx-auto">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>
        <div class="field mb-4">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full rounded-lg bg-gray-50" %>
        </div>
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Oczekiwanie na potwierdzenie dla: <%= resource.unconfirmed_email %></div>
        <% end %>
        <div class="field mb-4">
          <%= f.label :password, "Hasło" %> <i>(pozostaw puste jeśli nie chcesz go zmieniać)</i><br />
          <%= f.password_field :password, autocomplete: "new-password", class: "w-full rounded-lg bg-gray-50" %>
          <% if @minimum_password_length %>
            <br />
            <em>Minimum <%= @minimum_password_length %> znaków</em>
          <% end %>
        </div>
        <div class="field mb-4">
          <%= f.label :password_confirmation, "Potwierdzenie hasła" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full rounded-lg bg-gray-50" %>
        </div>
        <div class="field mb-4">
          <%= f.label :current_password, "Aktualne hasło" %> <i>(potrzebujemy Twojego aktualnego hasła, aby potwierdzić zmiany)</i><br />
          <%= f.password_field :current_password, autocomplete: "current-password", class: "w-full rounded-lg bg-gray-50" %>
        </div>
        <div class="actions">
          <%= f.submit "Aktualizuj", class: "bg-blue-500 w-full p-2 text-blue-100 rounded-lg cursor-pointer hover:bg-blue-700 hover:text-blue-300" %>
        </div>
      <% end %>
      <br/>
      <%= link_to "Powrót", :back, class: "text-lg" %>
      <div class="flex gap-2 items-center mt-8 mx-auto">Niezadowolony? <%= button_to "Usuń moje konto", registration_path(resource_name), data: { confirm: "Czy jesteś pewien?", turbo_confirm: "Czy jesteś pewien?" }, method: :delete, class: "bg-red-500 p-2 rounded-lg text-red-100" %></div>
    </div>
  </div>
</div>